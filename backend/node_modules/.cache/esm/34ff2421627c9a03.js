let mongoose;_627‍.x([["default",()=>_627‍.o]]);_627‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);
const { Schema, model } = mongoose;

const historicalDataModel = model(
  'HistoricalData',
  new Schema({
    stockIndex: String,
    startDate: String,
    endDate: String,
    stockArray: [{
      date: String,
      low: Number,
      high: Number,
      open: Number,
      close: Number,
      volume: Number,
      VWAP: Number,
    }],
  }),   
);

_627‍.d(historicalDataModel);
