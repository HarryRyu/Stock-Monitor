let historicalDataModel;_20c‍.x([["default",()=>historicalDataService]]);_20c‍.w("../model/index.js",[["default",["historicalDataModel"],function(v){historicalDataModel=v}]]);

               function historicalDataService(){
    const serviceCalls = {
        create: (stockIndex, startDate, endDate, array) => {
            _20c‍.g.console.log(stockIndex, startDate, endDate, array)
            return new Promise((resolve, reject) => {
                historicalDataModel.create({ 
                    stockIndex: stockIndex, 
                    startDate: startDate, 
                    endDate: endDate, 
                    stockArray: array,
                })
                .then(res => resolve(res))
                .catch(err => reject(err))
            })
        },
        findHistoricalData: (stockIndex) => {
            return new Promise((resolve, reject) => {
                historicalDataModel.find({ stockIndex: stockIndex})
                .then(res => resolve(res))
                .catch(err => reject(err))
            })
        }
    }

    return serviceCalls;
}